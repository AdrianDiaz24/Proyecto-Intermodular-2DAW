## Instrucciones de ejecucion 

1. cd frontend
2. ng serve
3. cd ../
4. cd backend
5. mvn spring-boot:run
6. localhost:4200

## Jerarquia de componentes

Se añado al menu del header la ruta /notas, para ir al componente raiz de las notas en la carpeta pages, en este se llama al segundo componente que es elinput, la idea en si erar crear un apartado donde los usuarios pudieran guardar sus notas, aunque sinceramente se han añadido correctamente la ruta, la modificacion del header, la creacion de los componentes y colocar el html basico para el input por el momento carece de funcionalidad.