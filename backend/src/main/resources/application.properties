# Configuracion de Spring Boot - Backend API REST
server.port=${PORT:8081}
server.servlet.context-path=/

# ===== Base de datos MySQL (CleverCloud) =====
spring.datasource.url=${DB_URL:jdbc:mysql://btx43wbqsgrwvrwdxyfo-mysql.services.clever-cloud.com:3306/btx43wbqsgrwvrwdxyfo?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USER:ugnnns6gniyuuxjm}
spring.datasource.password=${DB_PASS:bW3ymxU8H14L4kEnkRJf}

# JPA/Hibernate - MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Cargar datos iniciales
spring.sql.init.mode=never
spring.sql.init.data-locations=classpath:data.sql

# JWT Configuration
jwt.secret=${JWT_SECRET:clave_secreta_por_defecto_para_desarrollo_local_256bits}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Logging
logging.level.root=INFO
logging.level.com.proyecto.backend=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT Configuration
jwt.secret=${JWT_SECRET:clave_secreta_por_defecto_para_desarrollo_local_256bits}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Logging
logging.level.root=INFO
logging.level.com.proyecto.backend=DEBUG
logging.level.org.springframework.security=DEBUG

