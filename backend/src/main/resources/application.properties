# Configuración de Spring Boot

# Configuración del servidor
server.port=8080

# Configuración de la base de datos H2 (desarrollo)
spring.datasource.url=jdbc:h2:mem:reparaciones_db
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Habilitar consola H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Configuración JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuración para MySQL (producción - descomenta y modifica según tu configuración)
# spring.datasource.url=jdbc:mysql://localhost:3306/reparaciones_db
# spring.datasource.username=tu_usuario
# spring.datasource.password=tu_contraseña
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

